# Deployment

Deploy the contents of `dist/` to any static host. Configure the route as a portfolio demo and retain `noindex,nofollow` unless UVP intentionally indexes the portfolio wrapper. No environment variables, database, or server runtime are required.
